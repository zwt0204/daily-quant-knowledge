# Day 12 - 手写最简单回测

- 日期：2026-04-07
- 主题：手写最简单回测

## 今天学什么

今天先别上框架，自己手写一个最基础的回测链路。
你要先把下面这几步连起来：

1. 有信号
2. 把信号错位成持仓
3. 计算每日收益率
4. 算策略收益
5. 累计成净值

这一步一旦写通，后面完整脚本就有底了。

## 20-40 分钟任务

1. 准备一列收盘价
2. 算均线和信号
3. 用 `shift(1)` 生成 `position`
4. 算 `ret`
5. 算 `strategy_ret`
6. 算 `equity`

## 最小代码示例

```python
import pandas as pd

df = pd.DataFrame({'close': [10, 10.1, 10.3, 10.2, 10.5, 10.8, 10.7]})
df['ma3'] = df['close'].rolling(3).mean()
df['ma5'] = df['close'].rolling(5).mean()
df['signal'] = (df['ma3'] > df['ma5']).astype(int)
df['position'] = df['signal'].shift(1).fillna(0)
df['ret'] = df['close'].pct_change().fillna(0)
df['strategy_ret'] = df['position'] * df['ret']
df['equity'] = (1 + df['strategy_ret']).cumprod()
print(df[['close', 'signal', 'position', 'equity']])
```

## 打卡检查题

今天的检查题是：
**为什么 `position` 不能直接等于当天的 `signal`，而要先 `shift(1)`？**
